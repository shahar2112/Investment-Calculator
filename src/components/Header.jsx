export default function Header({header}) {
   return(
    <header id="header">
        <img src="investment-calculator-logo.png" alt="Logo showing a money bag"></img>
        <h1>{header}</h1>
    </header>
   );    
}